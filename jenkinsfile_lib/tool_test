#!/usr/bin/env groovy

/* import shared library */
@Library('jenkins-dsl')_

pipeline {
  agent {
    label "worker"
  }
  tools {
      terraform 'terraform'
    }
  
  stages {
    stage('Code Checkout from GitHub') {
      steps {
        container('worker') {
              git branch: "master",
              credentialsId: "github",
              url: "git@github.com:andycockers/gcp-automation.git"
        }
            }
}

stage('Test Terraform Install') {
      steps {
        container('worker') {
          
          sh 'terraform'
    
         
      
    }
  }
}
}
}

