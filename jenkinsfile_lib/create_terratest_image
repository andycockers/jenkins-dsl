#!/usr/bin/env groovy

/* import shared library */
@Library('jenkins-dsl')_

pipeline {
  
  stages {
    stage('Initialisation') {
      steps {
        
          
          packer_test()
        }
      }
    }
  }

