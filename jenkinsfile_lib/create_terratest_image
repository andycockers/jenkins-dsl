#!/usr/bin/env groovy

/* import shared library */
@Library('jenkins-dsl')_

pipeline {
  agent {
    node {
      label 'swarm'
    }
  }
  
  stages {
    stage('Initialisation') {
      steps {
        
          
          packer_test()
        }
      }
    }
  }

